def validate_pin(pin):
    g=bool
    for i in range(0,len(pin),1):
        if pin[i].isdigit()!=True:
            return False
    if len(pin)==4 or len(pin)==6:
        return True
    else:
        return False
